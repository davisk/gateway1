language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: "pip install -r install/requirements.txt"
# command to run tests
#script: python manage.py test
script: echo "success"
